分层提取原则
提取util层：
1、将与业务无关的的底层方法封装为util层(核心层)
2、例如：数据驱动中的文件读取，浏览器操作、元素定位方法等等
提取task层：
1、与业务相关的公共方法或者通用放在Task层中(可复用代码层)
2、例如：网站登录、注销、常用操作,參數操作
其他层提取：
1、通过对Element的整理，使用数据文件或Properties文件维护所有元素
2、数据层文件定义，配置文件整理
moco-runner基本操作说明
